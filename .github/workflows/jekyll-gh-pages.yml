jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Debug File Structure 🐛 (Run this first)
        run: |
          echo "--- Listing files in Repo Root ---"
          ls -Rla .
          echo "--- Listing files inside tic-tac-toe folder ---"
          # This command will fail if the folder 'tic-tac-toe' does not exist at the root
          ls -Rla ./tic-tac-toe

      # TEMPORARILY COMMENT OUT THE FAILING STEP
      # - name: 📦 Upload Artifact
      #   uses: actions/upload-pages-artifact@v3
      #   with:
      #     path: './tic-tac-toe/dist'

  deploy:
    # Set 'needs: []' so deploy doesn't run, or simply remove the deploy job for now
    # deployment can only run if build is successful
    runs-on: ubuntu-latest
    needs: []
    steps:
      - run: echo "Skipping deploy for debug"
